# Local Development Environment Variables
# Copy this to .env.local and fill in your values

# Enable local development mode (uses local kubeconfig)
LOCAL_RUN=true

# Service account token for cluster-level operations
# Required for namespace listing and provisioning
# Get this from your cluster:
#   OpenShift: SERVICE_ACCOUNT_TOKEN=$(oc whoami -t)
#   Kubernetes: SERVICE_ACCOUNT_TOKEN=$(kubectl config view --raw -o jsonpath='{.users[0].user.token}')
SERVICE_ACCOUNT_TOKEN=your-cluster-admin-token-here

# Namespace template (optional, defaults to 'che-<username>')
NAMESPACE_TEMPLATE=che-<username>

# API endpoint (optional, defaults to 'http://localhost:8080')
CHE_API_ENDPOINT=http://localhost:8080
